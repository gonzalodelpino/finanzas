/** @type {import('next').NextConfig} */
const nextConfig = {
  // Aquí puedes añadir otras configuraciones si lo necesitas, pero `experimental.turbo` no hace falta.
};

module.exports = nextConfig;

